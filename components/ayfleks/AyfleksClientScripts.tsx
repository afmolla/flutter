"use client";

import Script from "next/script";

/** Font Awesome (sosyal ikonlar + hamburger) — ayfleks.com ile aynı kit */
export function AyfleksClientScripts() {
  return <Script src="https://kit.fontawesome.com/c384e56078.js" strategy="afterInteractive" crossOrigin="anonymous" />;
}
